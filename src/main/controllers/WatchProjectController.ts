import { WatchProject } from '../../db/entity/WatchProject';
import { WatchProjectVO } from '../../db/service/WatchProjectService';

// const openProjectByPath = async (path: string):Promise<WatchProjectVO> => {
//     // 判断是文件还是文件夹
//     // 如果是文件夹，需要遍历文件夹下的所有视频文件
//     // 如果是文件，需要判断文件类型
//     // 如果是视频文件，需要获取字幕文件
//
//     const isDirectory = false;
//
// }
//
// const openProject = async (project_key: string):Promise<WatchProjectVO> => {
//     //todo
// }
