# 临时触发器：在修复分支上干跑完整链路（package_only，不上传 Release），
# 验证运行时矩阵断言、静态 CRT 构建、download.mjs 的 Windows 安装路径与 app 打包；
# 验证完成后删除本文件
name: Dry run full package
on:
  push:
    branches: [fix/win-whisper-runtime-deps]
jobs:
  package:
    uses: ./.github/workflows/release.yml
    with:
      package_only: true
