# Log level for main process
DP_LOG_LEVEL=debug

# Log level for renderer (Vite-injected)
VITE_DP_LOG_LEVEL=debug

# Optional log tag filter (comma-separated; use * or all to disable filtering)
DP_LOG_TAGS=subtitle,whisper
